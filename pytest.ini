# pytest.ini
[pytest]
addopts = -v --tb=short
testpaths = tests
python_files = test_*.py
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (slow, require services like database)
asyncio_mode = auto